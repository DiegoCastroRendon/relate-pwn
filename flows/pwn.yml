title: "Pwn Flow"
description: |
  Prueba rápida de render.

rules:
  grade_identifier: null
  start:
    - may_start_new_session: true
      may_list_existing_sessions: true
  access:
    - permissions: [view, submit_answer, end_session]

pages:
  - type: Page
    id: jinja
    content: |
      {{ 7*7 }}

